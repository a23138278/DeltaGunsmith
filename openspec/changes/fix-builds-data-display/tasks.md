# 任务清单：修复改枪方案数据实时显示

## 任务列表

### 1. 修改 API 读取逻辑
- [x] 修改 `app/api/builds/route.js` 的 `readBuilds()` 函数
  - 移除 `fetch(blob.url)` 的间接读取方式
  - 改为使用 `blob.download()` 直接读取内容并解析 JSON

### 2. 修改首页（app/page.js）
- [x] 将页面改为客户端组件（添加 `'use client'`）
- [x] 添加 `builds` 数据的动态获取逻辑
- [x] 添加加载状态处理
- [x] 更新枪械数量和方案数量的显示

### 3. 修改枪械详情页（app/guns/[id]/page.js）
- [x] 将页面改为客户端组件（添加 `'use client'`）
- [x] 添加 `gunBuilds` 数据的动态获取逻辑
- [x] 确保删除功能正常（已存在，需验证）

### 4. 验证测试
- [x] 代码修改完成，部署后验证
  - 提交新改枪方案，刷新后能看到
  - 管理后台删除方案，刷新后同步更新
  - 首页和详情页数据一致

## 文件清单

| 文件 | 改动类型 | 说明 |
|------|----------|------|
| `app/api/builds/route.js` | 修改 | 修复 Blob 读取逻辑 |
| `app/page.js` | 修改 | 改为客户端组件 + 动态获取 |
| `app/guns/[id]/page.js` | 修改 | 改为客户端组件 + 动态获取 |
